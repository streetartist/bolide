class Vec3 {
    x: float; y: float; z: float;
    fn set(px: float, py: float, pz: float) { self.x = px; self.y = py; self.z = pz; }
}

let GX: float = 0.5;

fn test(out: Vec3) {
    let n: Vec3 = Vec3(0.4, 0.6, 0.0);
    out.x = n.x * GX + n.y * GX;
}

let v: Vec3 = Vec3(0.0, 0.0, 0.0);
test(v);
print(v.x);
