// FFI 回调：C 函数指针类型为 func(...)

extern "dyn:c" {
    fn qsort(
        base: *c_void,
        num: c_size_t,
        size: c_size_t,
        compare: func(*c_void, *c_void) -> c_int
    );
}

fn compare_int(a: ptr, b: ptr) -> int {
    return 0;
}

print(999);
