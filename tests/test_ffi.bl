// 测试 FFI 调用 C 库（跨平台逻辑库名 + 统一 C 类型写法）

extern "dyn:c" {
    fn abs(x: c_int) -> c_int;
}

extern "dyn:m" {
    fn sqrt(x: f64) -> f64;
}

// 测试 abs
let a: int = abs(-42);
print(a);

// 测试 sqrt
let b: float = sqrt(16.0);
print(b);

print(999);
