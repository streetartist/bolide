fn take_owned(owned x: int) {
    print("got: "); print(x); print("\n");
}

fn test_local_var() {
    var a: int = 42;
    take_owned(a);
    a = 99;   // reassign after move?
    print("done\n");
}

test_local_var();
