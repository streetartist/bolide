// 测试 spawn select - 并行竞争等待

fn fast_task() -> int {
    return 10;
}

fn slow_task() -> int {
    return 20;
}

// 竞争等待，第一个完成的获胜
spawn select {
    result = fast_task() => {
        print(result);
    }
    result2 = slow_task() => {
        print(result2);
    }
}

print(999);
